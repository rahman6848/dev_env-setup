# Install "git", "curl", "wget", "nodejs", "MongoDB", "Redis", "Sublime Text", "ATOM", "Chrome", "Docker"
After cloning this repository, run ```sudo ./dev_env-setup.sh```
# To Start Services
To Start "MongoDB", run ```sudo ./start-services.sh```

